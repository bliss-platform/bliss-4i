//Pure data in bliss : there are three layout specifiers

data name; //pure 64 bit data
data type[] name; //array data of type
data structure {
	
} //a well rounded structure

// everything else is derived from all these.
// builtin raw types - all mathematical operators are defined over these

u8 a;
u16